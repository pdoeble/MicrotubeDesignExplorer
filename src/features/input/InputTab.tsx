export function InputTab() {
  return (
    <section aria-labelledby="input-heading">
      <h2 id="input-heading">Input</h2>
      <p className="placeholder-note" role="status">
        The two-cooler input workflow (geometry, operating conditions, group linking) is implemented
        in milestone M5 after the contracts freeze (M2).
      </p>
    </section>
  );
}
